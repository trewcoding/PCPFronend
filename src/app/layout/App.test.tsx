import React from "react";
import { getByTitle, render, screen } from '@testing-library/react';
import App from './App';

test("NavBar renders on App rendering", () => {
    // render(<App />);
    // const navBar = screen.findByRole('./NavBar')
    // expect(navBar).toContainElement("navBar")
})