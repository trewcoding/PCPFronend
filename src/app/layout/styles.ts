import { createStyles, makeStyles } from '@mui/styles';

const useStyles = makeStyles(
  createStyles({
    appBarSize: {
      height:'80px',
      margin: '0px',
      padding: '0px'
    }

  }),
);
    
    
export default useStyles;




